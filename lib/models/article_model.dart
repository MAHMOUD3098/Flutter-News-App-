class ArticleModel {
  final String author, title, description, url, imgUrl, content, publishedAt;

  ArticleModel(this.author, this.title, this.description, this.url, this.imgUrl,
      this.content, this.publishedAt);
}
